// Strings are - immutable in javascript 
// some of the common example of Functions and Methods are as follows 
//1.  str.toUpperCase()
//2.  str.toLowerCase()
//3.  str.trim() // Removes WhiteSpaces 
// 4. str.slice(start, end) // returns Part of strings 
// 5. str1.concat(str2) // joins both the strings 
// 6. str.replace(searchVal, newVal);
// 7. str.charAt(idx);

